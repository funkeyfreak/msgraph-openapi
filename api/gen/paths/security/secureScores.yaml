get:
    operationId: security.ListSecureScores
    parameters:
        - $ref: '#/components/parameters/top'
        - $ref: '#/components/parameters/skip'
        - $ref: '#/components/parameters/search'
        - $ref: '#/components/parameters/filter'
        - $ref: '#/components/parameters/count'
        - description: Order items by property values
          explode: false
          in: query
          name: $orderby
          schema:
            items:
                enum:
                    - id
                    - id desc
                    - activeUserCount
                    - activeUserCount desc
                    - averageComparativeScores
                    - averageComparativeScores desc
                    - azureTenantId
                    - azureTenantId desc
                    - controlScores
                    - controlScores desc
                    - createdDateTime
                    - createdDateTime desc
                    - currentScore
                    - currentScore desc
                    - enabledServices
                    - enabledServices desc
                    - licensedUserCount
                    - licensedUserCount desc
                    - maxScore
                    - maxScore desc
                    - vendorInformation
                    - vendorInformation desc
                type: string
            type: array
          style: form
        - description: Select properties to be returned
          explode: false
          in: query
          name: $select
          schema:
            items:
                enum:
                    - id
                    - activeUserCount
                    - averageComparativeScores
                    - azureTenantId
                    - controlScores
                    - createdDateTime
                    - currentScore
                    - enabledServices
                    - licensedUserCount
                    - maxScore
                    - vendorInformation
                type: string
            type: array
          style: form
        - description: Expand related entities
          explode: false
          in: query
          name: $expand
          schema:
            items:
                enum:
                    - '*'
                type: string
            type: array
          style: form
    responses:
        "200":
            content:
                application/json:
                    schema:
                        properties:
                            value:
                                items:
                                    $ref: '#/components/schemas/microsoft.graph.secureScore'
                                type: array
                        title: Collection of secureScore
                        type: object
            description: Retrieved navigation property
        default:
            $ref: '#/components/responses/error'
    summary: Get secureScores from security
    tags:
        - security.secureScore
    x-ms-docs-operation-type: operation
post:
    operationId: security.CreateSecureScores
    requestBody:
        content:
            application/json:
                schema:
                    $ref: '#/components/schemas/microsoft.graph.secureScore'
        description: New navigation property
        required: true
    responses:
        "201":
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/microsoft.graph.secureScore'
            description: Created navigation property.
        default:
            $ref: '#/components/responses/error'
    summary: Create new navigation property to secureScores for security
    tags:
        - security.secureScore
    x-ms-docs-operation-type: operation
