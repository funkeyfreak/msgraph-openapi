get:
    operationId: planner.GetPlans
    parameters:
        - description: 'key: plannerPlan-id of plannerPlan'
          in: path
          name: plannerPlan-id
          required: true
          schema:
            type: string
          x-ms-docs-key-type: plannerPlan
        - description: Select properties to be returned
          explode: false
          in: query
          name: $select
          schema:
            items:
                enum:
                    - id
                    - createdBy
                    - createdDateTime
                    - owner
                    - title
                    - buckets
                    - details
                    - tasks
                type: string
            type: array
          style: form
        - description: Expand related entities
          explode: false
          in: query
          name: $expand
          schema:
            items:
                enum:
                    - '*'
                    - buckets
                    - details
                    - tasks
                type: string
            type: array
          style: form
    responses:
        "200":
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/microsoft.graph.plannerPlan'
            description: Retrieved navigation property
        default:
            $ref: '#/components/responses/error'
    summary: Get plans from planner
    tags:
        - planner.plannerPlan
    x-ms-docs-operation-type: operation
patch:
    operationId: planner.UpdatePlans
    parameters:
        - description: 'key: plannerPlan-id of plannerPlan'
          in: path
          name: plannerPlan-id
          required: true
          schema:
            type: string
          x-ms-docs-key-type: plannerPlan
    requestBody:
        content:
            application/json:
                schema:
                    $ref: '#/components/schemas/microsoft.graph.plannerPlan'
        description: New navigation property values
        required: true
    responses:
        "204":
            description: Success
        default:
            $ref: '#/components/responses/error'
    summary: Update the navigation property plans in planner
    tags:
        - planner.plannerPlan
    x-ms-docs-operation-type: operation
