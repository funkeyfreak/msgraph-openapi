post:
    operationId: servicePrincipals.addPassword
    parameters:
        - description: 'key: servicePrincipal-id of servicePrincipal'
          in: path
          name: servicePrincipal-id
          required: true
          schema:
            type: string
          x-ms-docs-key-type: servicePrincipal
    requestBody:
        $ref: '#/components/requestBodies/applications.addPasswordBody'
    responses:
        "200":
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/microsoft.graph.passwordCredential'
            description: Success
        default:
            $ref: '#/components/responses/error'
    summary: Invoke action addPassword
    tags:
        - servicePrincipals.Actions
    x-ms-docs-operation-type: action
