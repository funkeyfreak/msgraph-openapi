get:
    operationId: me.ListCalendarGroups
    parameters:
        - $ref: '#/components/parameters/top'
        - $ref: '#/components/parameters/skip'
        - $ref: '#/components/parameters/filter'
        - $ref: '#/components/parameters/count'
        - description: Order items by property values
          explode: false
          in: query
          name: $orderby
          schema:
            items:
                enum:
                    - id
                    - id desc
                    - changeKey
                    - changeKey desc
                    - classId
                    - classId desc
                    - name
                    - name desc
                type: string
            type: array
          style: form
        - description: Select properties to be returned
          explode: false
          in: query
          name: $select
          schema:
            items:
                enum:
                    - id
                    - changeKey
                    - classId
                    - name
                    - calendars
                type: string
            type: array
          style: form
    responses:
        "200":
            content:
                application/json:
                    schema:
                        properties:
                            value:
                                items:
                                    $ref: '#/components/schemas/microsoft.graph.calendarGroup'
                                type: array
                        title: Collection of calendarGroup
                        type: object
            description: Retrieved navigation property
        default:
            $ref: '#/components/responses/error'
    summary: Get calendarGroups from me
    tags:
        - me.calendarGroup
    x-ms-docs-operation-type: operation
post:
    operationId: me.CreateCalendarGroups
    requestBody:
        $ref: '#/components/requestBodies/microsoft.graph.calendarGroup2'
    responses:
        "201":
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/microsoft.graph.calendarGroup'
            description: Created navigation property.
        default:
            $ref: '#/components/responses/error'
    summary: Create new navigation property to calendarGroups for me
    tags:
        - me.calendarGroup
    x-ms-docs-operation-type: operation
