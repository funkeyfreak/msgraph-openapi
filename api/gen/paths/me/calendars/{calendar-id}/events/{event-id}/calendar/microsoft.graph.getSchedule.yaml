post:
    operationId: me.calendars.events.calendar.getSchedule
    parameters:
        - description: 'key: calendar-id of calendar'
          in: path
          name: calendar-id
          required: true
          schema:
            type: string
          x-ms-docs-key-type: calendar
        - description: 'key: event-id of event'
          in: path
          name: event-id
          required: true
          schema:
            type: string
          x-ms-docs-key-type: event
    requestBody:
        $ref: '#/components/requestBodies/groups.calendar.calendarView.calendar.getScheduleBody'
    responses:
        "200":
            content:
                application/json:
                    schema:
                        items: {}
                        type: array
            description: Success
        default:
            $ref: '#/components/responses/error'
    summary: Invoke action getSchedule
    tags:
        - me.Actions
    x-ms-docs-operation-type: action
