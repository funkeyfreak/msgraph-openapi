get:
    operationId: me.onenote.ListOperations
    parameters:
        - $ref: '#/components/parameters/top'
        - $ref: '#/components/parameters/skip'
        - $ref: '#/components/parameters/search'
        - $ref: '#/components/parameters/filter'
        - $ref: '#/components/parameters/count'
        - description: Order items by property values
          explode: false
          in: query
          name: $orderby
          schema:
            items:
                enum:
                    - id
                    - id desc
                    - createdDateTime
                    - createdDateTime desc
                    - lastActionDateTime
                    - lastActionDateTime desc
                    - status
                    - status desc
                    - error
                    - error desc
                    - percentComplete
                    - percentComplete desc
                    - resourceId
                    - resourceId desc
                    - resourceLocation
                    - resourceLocation desc
                type: string
            type: array
          style: form
        - description: Select properties to be returned
          explode: false
          in: query
          name: $select
          schema:
            items:
                enum:
                    - id
                    - createdDateTime
                    - lastActionDateTime
                    - status
                    - error
                    - percentComplete
                    - resourceId
                    - resourceLocation
                type: string
            type: array
          style: form
        - description: Expand related entities
          explode: false
          in: query
          name: $expand
          schema:
            items:
                enum:
                    - '*'
                type: string
            type: array
          style: form
    responses:
        "200":
            content:
                application/json:
                    schema:
                        properties:
                            value:
                                items:
                                    $ref: '#/components/schemas/microsoft.graph.onenoteOperation'
                                type: array
                        title: Collection of onenoteOperation
                        type: object
            description: Retrieved navigation property
        default:
            $ref: '#/components/responses/error'
    summary: Get operations from me
    tags:
        - me.onenote
    x-ms-docs-operation-type: operation
post:
    operationId: me.onenote.CreateOperations
    requestBody:
        $ref: '#/components/requestBodies/microsoft.graph.onenoteOperation'
    responses:
        "201":
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/microsoft.graph.onenoteOperation'
            description: Created navigation property.
        default:
            $ref: '#/components/responses/error'
    summary: Create new navigation property to operations for me
    tags:
        - me.onenote
    x-ms-docs-operation-type: operation
