get:
    operationId: me.ListLicenseDetails
    parameters:
        - $ref: '#/components/parameters/top'
        - $ref: '#/components/parameters/skip'
        - $ref: '#/components/parameters/search'
        - $ref: '#/components/parameters/filter'
        - $ref: '#/components/parameters/count'
        - description: Order items by property values
          explode: false
          in: query
          name: $orderby
          schema:
            items:
                enum:
                    - id
                    - id desc
                    - servicePlans
                    - servicePlans desc
                    - skuId
                    - skuId desc
                    - skuPartNumber
                    - skuPartNumber desc
                type: string
            type: array
          style: form
        - description: Select properties to be returned
          explode: false
          in: query
          name: $select
          schema:
            items:
                enum:
                    - id
                    - servicePlans
                    - skuId
                    - skuPartNumber
                type: string
            type: array
          style: form
        - description: Expand related entities
          explode: false
          in: query
          name: $expand
          schema:
            items:
                enum:
                    - '*'
                type: string
            type: array
          style: form
    responses:
        "200":
            content:
                application/json:
                    schema:
                        properties:
                            value:
                                items:
                                    $ref: '#/components/schemas/microsoft.graph.licenseDetails'
                                type: array
                        title: Collection of licenseDetails
                        type: object
            description: Retrieved navigation property
        default:
            $ref: '#/components/responses/error'
    summary: Get licenseDetails from me
    tags:
        - me.licenseDetails
    x-ms-docs-operation-type: operation
post:
    operationId: me.CreateLicenseDetails
    requestBody:
        $ref: '#/components/requestBodies/microsoft.graph.licenseDetails'
    responses:
        "201":
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/microsoft.graph.licenseDetails'
            description: Created navigation property.
        default:
            $ref: '#/components/responses/error'
    summary: Create new navigation property to licenseDetails for me
    tags:
        - me.licenseDetails
    x-ms-docs-operation-type: operation
