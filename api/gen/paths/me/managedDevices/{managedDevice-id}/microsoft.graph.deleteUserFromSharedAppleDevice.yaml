post:
    operationId: me.managedDevices.deleteUserFromSharedAppleDevice
    parameters:
        - description: 'key: managedDevice-id of managedDevice'
          in: path
          name: managedDevice-id
          required: true
          schema:
            type: string
          x-ms-docs-key-type: managedDevice
    requestBody:
        $ref: '#/components/requestBodies/deviceManagement.detectedApps.managedDevices.deleteUserFromSharedAppleDeviceBody'
    responses:
        "204":
            description: Success
        default:
            $ref: '#/components/responses/error'
    summary: Invoke action deleteUserFromSharedAppleDevice
    tags:
        - me.Actions
    x-ms-docs-operation-type: action
