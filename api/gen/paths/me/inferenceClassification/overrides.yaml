get:
    operationId: me.inferenceClassification.ListOverrides
    parameters:
        - $ref: '#/components/parameters/top'
        - $ref: '#/components/parameters/skip'
        - $ref: '#/components/parameters/filter'
        - $ref: '#/components/parameters/count'
        - description: Order items by property values
          explode: false
          in: query
          name: $orderby
          schema:
            items:
                enum:
                    - id
                    - id desc
                    - classifyAs
                    - classifyAs desc
                    - senderEmailAddress
                    - senderEmailAddress desc
                type: string
            type: array
          style: form
        - description: Select properties to be returned
          explode: false
          in: query
          name: $select
          schema:
            items:
                enum:
                    - id
                    - classifyAs
                    - senderEmailAddress
                type: string
            type: array
          style: form
    responses:
        "200":
            content:
                application/json:
                    schema:
                        properties:
                            value:
                                items:
                                    $ref: '#/components/schemas/microsoft.graph.inferenceClassificationOverride'
                                type: array
                        title: Collection of inferenceClassificationOverride
                        type: object
            description: Retrieved navigation property
        default:
            $ref: '#/components/responses/error'
    summary: Get overrides from me
    tags:
        - me.inferenceClassification
    x-ms-docs-operation-type: operation
post:
    operationId: me.inferenceClassification.CreateOverrides
    requestBody:
        $ref: '#/components/requestBodies/microsoft.graph.inferenceClassificationOverride2'
    responses:
        "201":
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/microsoft.graph.inferenceClassificationOverride'
            description: Created navigation property.
        default:
            $ref: '#/components/responses/error'
    summary: Create new navigation property to overrides for me
    tags:
        - me.inferenceClassification
    x-ms-docs-operation-type: operation
