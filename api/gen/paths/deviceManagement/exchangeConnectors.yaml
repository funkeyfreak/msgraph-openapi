get:
    operationId: deviceManagement.ListExchangeConnectors
    parameters:
        - $ref: '#/components/parameters/top'
        - $ref: '#/components/parameters/skip'
        - $ref: '#/components/parameters/search'
        - $ref: '#/components/parameters/filter'
        - $ref: '#/components/parameters/count'
        - description: Order items by property values
          explode: false
          in: query
          name: $orderby
          schema:
            items:
                enum:
                    - id
                    - id desc
                    - connectorServerName
                    - connectorServerName desc
                    - exchangeAlias
                    - exchangeAlias desc
                    - exchangeConnectorType
                    - exchangeConnectorType desc
                    - exchangeOrganization
                    - exchangeOrganization desc
                    - lastSyncDateTime
                    - lastSyncDateTime desc
                    - primarySmtpAddress
                    - primarySmtpAddress desc
                    - serverName
                    - serverName desc
                    - status
                    - status desc
                    - version
                    - version desc
                type: string
            type: array
          style: form
        - description: Select properties to be returned
          explode: false
          in: query
          name: $select
          schema:
            items:
                enum:
                    - id
                    - connectorServerName
                    - exchangeAlias
                    - exchangeConnectorType
                    - exchangeOrganization
                    - lastSyncDateTime
                    - primarySmtpAddress
                    - serverName
                    - status
                    - version
                type: string
            type: array
          style: form
        - description: Expand related entities
          explode: false
          in: query
          name: $expand
          schema:
            items:
                enum:
                    - '*'
                type: string
            type: array
          style: form
    responses:
        "200":
            content:
                application/json:
                    schema:
                        properties:
                            value:
                                items:
                                    $ref: '#/components/schemas/microsoft.graph.deviceManagementExchangeConnector'
                                type: array
                        title: Collection of deviceManagementExchangeConnector
                        type: object
            description: Retrieved navigation property
        default:
            $ref: '#/components/responses/error'
    summary: Get exchangeConnectors from deviceManagement
    tags:
        - deviceManagement.deviceManagementExchangeConnector
    x-ms-docs-operation-type: operation
post:
    operationId: deviceManagement.CreateExchangeConnectors
    requestBody:
        content:
            application/json:
                schema:
                    $ref: '#/components/schemas/microsoft.graph.deviceManagementExchangeConnector'
        description: New navigation property
        required: true
    responses:
        "201":
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/microsoft.graph.deviceManagementExchangeConnector'
            description: Created navigation property.
        default:
            $ref: '#/components/responses/error'
    summary: Create new navigation property to exchangeConnectors for deviceManagement
    tags:
        - deviceManagement.deviceManagementExchangeConnector
    x-ms-docs-operation-type: operation
