get:
    operationId: deviceManagement.ListDetectedApps
    parameters:
        - $ref: '#/components/parameters/top'
        - $ref: '#/components/parameters/skip'
        - $ref: '#/components/parameters/search'
        - $ref: '#/components/parameters/filter'
        - $ref: '#/components/parameters/count'
        - description: Order items by property values
          explode: false
          in: query
          name: $orderby
          schema:
            items:
                enum:
                    - id
                    - id desc
                    - deviceCount
                    - deviceCount desc
                    - displayName
                    - displayName desc
                    - sizeInByte
                    - sizeInByte desc
                    - version
                    - version desc
                type: string
            type: array
          style: form
        - description: Select properties to be returned
          explode: false
          in: query
          name: $select
          schema:
            items:
                enum:
                    - id
                    - deviceCount
                    - displayName
                    - sizeInByte
                    - version
                    - managedDevices
                type: string
            type: array
          style: form
        - description: Expand related entities
          explode: false
          in: query
          name: $expand
          schema:
            items:
                enum:
                    - '*'
                    - managedDevices
                type: string
            type: array
          style: form
    responses:
        "200":
            content:
                application/json:
                    schema:
                        properties:
                            value:
                                items:
                                    $ref: '#/components/schemas/microsoft.graph.detectedApp'
                                type: array
                        title: Collection of detectedApp
                        type: object
            description: Retrieved navigation property
        default:
            $ref: '#/components/responses/error'
    summary: Get detectedApps from deviceManagement
    tags:
        - deviceManagement.detectedApp
    x-ms-docs-operation-type: operation
post:
    operationId: deviceManagement.CreateDetectedApps
    requestBody:
        content:
            application/json:
                schema:
                    $ref: '#/components/schemas/microsoft.graph.detectedApp'
        description: New navigation property
        required: true
    responses:
        "201":
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/microsoft.graph.detectedApp'
            description: Created navigation property.
        default:
            $ref: '#/components/responses/error'
    summary: Create new navigation property to detectedApps for deviceManagement
    tags:
        - deviceManagement.detectedApp
    x-ms-docs-operation-type: operation
