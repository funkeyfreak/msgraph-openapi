post:
    operationId: applications.addPassword
    parameters:
        - description: 'key: application-id of application'
          in: path
          name: application-id
          required: true
          schema:
            type: string
          x-ms-docs-key-type: application
    requestBody:
        $ref: '#/components/requestBodies/applications.addPasswordBody'
    responses:
        "200":
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/microsoft.graph.passwordCredential'
            description: Success
        default:
            $ref: '#/components/responses/error'
    summary: Invoke action addPassword
    tags:
        - applications.Actions
    x-ms-docs-operation-type: action
