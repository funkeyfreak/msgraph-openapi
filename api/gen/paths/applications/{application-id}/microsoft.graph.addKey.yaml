post:
    operationId: applications.addKey
    parameters:
        - description: 'key: application-id of application'
          in: path
          name: application-id
          required: true
          schema:
            type: string
          x-ms-docs-key-type: application
    requestBody:
        $ref: '#/components/requestBodies/applications.addKeyBody'
    responses:
        "200":
            content:
                application/json:
                    schema:
                        $ref: '#/components/schemas/microsoft.graph.keyCredential'
            description: Success
        default:
            $ref: '#/components/responses/error'
    summary: Invoke action addKey
    tags:
        - applications.Actions
    x-ms-docs-operation-type: action
