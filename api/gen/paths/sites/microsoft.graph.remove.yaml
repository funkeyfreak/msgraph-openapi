post:
    operationId: sites.remove
    requestBody:
        $ref: '#/components/requestBodies/sites.addBody'
    responses:
        "200":
            content:
                application/json:
                    schema:
                        items: {}
                        type: array
            description: Success
        default:
            $ref: '#/components/responses/error'
    summary: Invoke action remove
    tags:
        - sites.Actions
    x-ms-docs-operation-type: action
