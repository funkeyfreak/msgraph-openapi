post:
    operationId: sites.onenote.notebooks.getNotebookFromWebUrl
    parameters:
        - description: 'key: site-id of site'
          in: path
          name: site-id
          required: true
          schema:
            type: string
          x-ms-docs-key-type: site
    requestBody:
        $ref: '#/components/requestBodies/groups.onenote.notebooks.getNotebookFromWebUrlBody'
    responses:
        "200":
            content:
                application/json:
                    schema: {}
            description: Success
        default:
            $ref: '#/components/responses/error'
    summary: Invoke action getNotebookFromWebUrl
    tags:
        - sites.Actions
    x-ms-docs-operation-type: action
