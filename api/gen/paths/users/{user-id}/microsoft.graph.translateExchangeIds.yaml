post:
    operationId: users.translateExchangeIds
    parameters:
        - description: 'key: user-id of user'
          in: path
          name: user-id
          required: true
          schema:
            type: string
          x-ms-docs-key-type: user
    requestBody:
        $ref: '#/components/requestBodies/me.translateExchangeIdsBody'
    responses:
        "200":
            content:
                application/json:
                    schema:
                        items:
                            $ref: '#/components/schemas/microsoft.graph.convertIdResult'
                        type: array
            description: Success
        default:
            $ref: '#/components/responses/error'
    summary: Invoke action translateExchangeIds
    tags:
        - users.Actions
    x-ms-docs-operation-type: action
